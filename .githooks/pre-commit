
#!/usr/bin/env bash

npx validate-branch-name -r '^(master|main|prod|develop)$|^(feature|fix|hotfix|release)/.+$'

# echo "formatting tf files"
# cd $(git rev-parse --show-toplevel)
# terraform  fmt -write=true -recursive | xargs git add 
